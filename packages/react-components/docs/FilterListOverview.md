---
title: FilterList
description: An OSDK-aware filter panel for ontology objects. Renders a list of filter controls — listogram, select, range, keyword search, and more — driven by aggregations against an object type or ObjectSet, and emits a composed where clause or filtered ObjectSet.
---

> **Beta** — exported from `@osdk/react-components/experimental/filter-list`.

## Usage

FilterList fetches its own facet data. Wrap your app in `OsdkProvider` and import the package styles once (see [Prerequisites](./Prerequisites.md) for the `@layer` order), then pass an object type and a set of filter definitions — aggregation counts, dropdowns, add/remove, and reset are wired up out of the box.

> **Note** — `@my/osdk` is a placeholder for **your generated SDK package** (e.g. `@your-app/sdk`). Replace it with the actual package name in your project.

```tsx
import { Employee } from "@my/osdk";
import { FilterList } from "@osdk/react-components/experimental/filter-list";

function EmployeeFilters() {
  return (
    <FilterList
      objectType={Employee}
      filterDefinitions={[
        { type: "PROPERTY", key: "department", filterComponent: "LISTOGRAM" },
        { type: "PROPERTY", key: "jobTitle", filterComponent: "LISTOGRAM" },
      ]}
    />
  );
}
```

## Props

This table is generated from the `FilterListProps` interface in [`FilterListApi.ts`](https://github.com/palantir/osdk-ts/blob/main/packages/react-components/src/filter-list/FilterListApi.ts). It lists the props declared directly on `FilterListProps`; for the shapes of the individual filter definitions (`PROPERTY`, `KEYWORD_SEARCH`, `HAS_LINK`, `LINKED_PROPERTY`, `CUSTOM`, `STATIC_VALUES`) and the available filter components, see the [full FilterList reference](https://github.com/palantir/osdk-ts/blob/main/packages/react-components/docs/FilterList.md).

<!-- AUTOGEN:props START src=src/filter-list/FilterListApi.ts interface=FilterListProps — generated by scripts/gen-props.mjs; run `pnpm --filter @osdk/react-components gen-props` -->

Type parameters: `Q extends ObjectTypeDefinition`

| Name                       | Type                                                                                 | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| -------------------------- | ------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `objectType`               | `Q`                                                                                  | **Required.** The object type definition for the objects being filtered. Used for metadata resolution (property types, display names).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `objectSet`                | `ObjectSet<Q>`                                                                       | Optional object set to scope aggregation queries. When omitted, aggregations run against the full object type.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `onFilterClauseChanged`    | `(newClause: WhereClause<Q>) => void`                                                | Called whenever the filter clause changes. FilterList owns filter state; this is how you read it out, e.g. to feed an `ObjectTable`'s `filter`.<br /><br />`LINKED_PROPERTY` filters are not represented in the clause — use `onEffectiveObjectSet` for those.                                                                                                                                                                                                                                                                                                                                                                                             |
| `title`                    | `ReactNode`                                                                          | Optional title to display in the filter list header                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `titleIcon`                | `React.ReactNode`                                                                    | Optional icon to display next to the title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `filterDefinitions`        | `Array<FilterDefinitionUnion<Q>>`                                                    | The definition for all supported filter items in the list If not supplied, all filterable properties will be available                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `onFilterStateChanged`     | `(definition: FilterDefinitionUnion<Q>, newState: FilterState) => void`              | Called when filter state changes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `onEffectiveObjectSet`     | `(objectSet: ObjectSet<Q>) => void`                                                  | Called with the filtered `ObjectSet` whenever filters change. Requires `objectSet` to be set.<br /><br />A linked filter only filters the set when its definition has `reverseLinkName`. Linked filters without it are skipped here; read their state from `onFilterStateChanged` instead.                                                                                                                                                                                                                                                                                                                                                                 |
| `showFilteredOutValues`    | `boolean`                                                                            | When `true`, facets render greyed-out count=0 rows for values present in the unfiltered data but excluded by other active filters. Defaults to `false`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `addFilterMode`            | `"controlled" \| "uncontrolled"`                                                     | **Deprecated** — Going away; visibility will always be managed internally. Seed it with `isVisible` on each `filterDefinitions` entry and observe changes with `onFilterVisibilityChange`. Controls how filter visibility (add/remove) is managed.<br /><br />- `"uncontrolled"` (default): FilterList manages visibility internally. An "Add filter" popover is rendered for filters with `isVisible: false`, and each visible filter shows a remove button. - `"controlled"`: The consumer manages which filters are visible via `filterDefinitions`. Filters with `isVisible: false` are excluded from the rendered list. Defaults to `"uncontrolled"`. |
| `onFilterAdded`            | `(filterKey: FilterKey<Q>, newDefinitions: Array<FilterDefinitionUnion<Q>>) => void` | Called after a filter is shown from the built-in "Add filter" popover.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `onFilterRemoved`          | `(filterKey: FilterKey<Q>) => void`                                                  | Called after a filter's remove button is clicked, once the filter is hidden and its state cleared.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `onFilterVisibilityChange` | `(newStates: Array<{ filterKey: FilterKey<Q>; isVisible: boolean; }>) => void`       | Called when filter visibility or ordering changes, i.e. when filters are reordered, added or removed via the built-in show/remove controls, or reset.<br /><br />Visible filters come first, in display order, followed by the hidden ones. Persist this array and feed it back as the order and `isVisible` of `filterDefinitions` to make reordering survive a remount.                                                                                                                                                                                                                                                                                  |
| `enableSorting`            | `boolean`                                                                            | Enable drag-and-drop reordering of filters. When `true`, drag handles are rendered and filters can be reordered.<br /><br />Reorder state is managed internally; persist `onFilterVisibilityChange` to track order across remounts. Defaults to `false`.                                                                                                                                                                                                                                                                                                                                                                                                   |
| `enableCollapse`           | `boolean`                                                                            | Whether the collapse/expand control is available. When `false` the panel is always expanded, no collapse control is rendered, and `collapsed` / `defaultCollapsed` are ignored. Defaults to `true`.                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `collapsed`                | `boolean`                                                                            | Controlled mode. When supplied, this prop is the source of truth for whether the panel is collapsed and the component keeps no internal state; re-render with a new value in response to `onCollapsedChange`.<br /><br />If both `collapsed` and `defaultCollapsed` are provided, `collapsed` takes precedence. Ignored when `enableCollapse` is `false`.                                                                                                                                                                                                                                                                                                  |
| `defaultCollapsed`         | `boolean`                                                                            | Uncontrolled mode. Seeds the panel's internal collapsed state; the component continues to own the state after mount, so later changes to this prop are ignored.<br /><br />If both `collapsed` and `defaultCollapsed` are provided, `collapsed` takes precedence. Ignored when `enableCollapse` is `false`. Defaults to `false`.                                                                                                                                                                                                                                                                                                                           |
| `onCollapsedChange`        | `(collapsed: boolean) => void`                                                       | Called whenever the collapsed state changes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `defaultFilterStates`      | `Map<string, FilterState>`                                                           | Seeds filter states from external storage, keyed by `getFilterKey`. Applied over the per-definition `defaultFilterState` seeds on mount, and FilterList owns the states from then on. Also the state the reset button restores to. Use `onFilterStateChanged` to persist changes back out. Defaults to `undefined (filters seed from their definitions alone)`.                                                                                                                                                                                                                                                                                            |
| `initialFilterStates`      | `Map<string, FilterState>`                                                           | **Deprecated** — Rename to `defaultFilterStates`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `showResetButton`          | `boolean`                                                                            | Show reset filters button in header                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `onReset`                  | `() => void`                                                                         | Called when reset button is clicked                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `showActiveFilterCount`    | `boolean`                                                                            | Show count of active filters in header                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `className`                | `string`                                                                             | Additional CSS class name                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `renderAddFilterButton`    | `() => React.ReactNode`                                                              | Custom render function for the "Add filter" button. Customizes the trigger element for the built-in add-filter popover; the popover behavior is handled automatically.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

<!-- AUTOGEN:props END -->

## Style API

FilterList is themed entirely through `--osdk-filter-list-*` CSS variables defined in `@osdk/react-components/styles.css`. Override them to restyle the panel without forking the component; scope overrides with the `className` prop, or set `--osdk-*` / `--bp-*` tokens at `:root` for global theming.

The tokens are grouped by surface — container, content, collapsed state, header, filter item (plus its drag handle, header actions, exclude dropdown, and overflow menu), the shared input / message / tag tokens, and per-component groups for checkbox list, toggle, listogram, range input, timeline, multi-select, date, single-select, text-tags, no-value, filtered-out items, skeleton loading, and the filter popover.

For the full list of tokens with their default values, see the [**Filter List** section of the CSS Variables reference](./CSSVariables.md#filter-list).

## Advanced

`FilterList` is the OSDK-aware component most consumers should use. For cases where you need to bring your own data fetching or assemble the panel from smaller pieces, the package also exports the OSDK-agnostic base component, the individual building blocks, and the headless hook that backs them. All are available from `@osdk/react-components/experimental/filter-list`.

### Base component

`BaseFilterList` is the OSDK-agnostic panel that renders the filter list chrome — header, reset, active-filter count, add/remove, drag-to-reorder, and collapse — from primitive props. It carries all of `FilterList`'s layout and interactions but does no data fetching or where-clause composition: you supply the filter states map, a `renderInput` callback for each filter's body, and `getFilterKey` / `getFilterLabel` accessors.

This table is generated from the `BaseFilterListProps` interface in [`BaseFilterListApi.ts`](https://github.com/palantir/osdk-ts/blob/main/packages/react-components/src/filter-list/base/BaseFilterListApi.ts).

<!-- AUTOGEN:props START src=src/filter-list/base/BaseFilterListApi.ts interface=BaseFilterListProps — generated by scripts/gen-props.mjs; run `pnpm --filter @osdk/react-components gen-props` -->

Type parameters: `D extends FilterDefinitionControls`

| Name                    | Type                                              | Description                                                                                                                                                                                                                                                                                                                                               |
| ----------------------- | ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `filterDefinitions`     | `Array<D>`                                        |                                                                                                                                                                                                                                                                                                                                                           |
| `filterStates`          | `Map<string, FilterState>`                        | **Required.**                                                                                                                                                                                                                                                                                                                                             |
| `onFilterStateChanged`  | `(filterKey: string, state: FilterState) => void` | **Required.**                                                                                                                                                                                                                                                                                                                                             |
| `renderInput`           | `RenderFilterInput<D>`                            | **Required.**                                                                                                                                                                                                                                                                                                                                             |
| `getFilterKey`          | `(definition: D) => string`                       | **Required.**                                                                                                                                                                                                                                                                                                                                             |
| `getFilterLabel`        | `(definition: D) => string`                       | **Required.**                                                                                                                                                                                                                                                                                                                                             |
| `getEmptyDisplayState`  | `(definition: D) => FilterState \| undefined`     | Display-only fallback state for a filter that has no stored state, used so capability-driven header controls (overflow … menu, search) render for empty/just-added filters. Never written into the filter-state map.                                                                                                                                      |
| `activeFilterCount`     | `number`                                          | **Required.**                                                                                                                                                                                                                                                                                                                                             |
| `onReset`               | `() => void`                                      |                                                                                                                                                                                                                                                                                                                                                           |
| `onFilterAdded`         | `() => void`                                      |                                                                                                                                                                                                                                                                                                                                                           |
| `onFilterRemoved`       | `(filterKey: string) => void`                     |                                                                                                                                                                                                                                                                                                                                                           |
| `onOrderChange`         | `(orderedKeys: string[]) => void`                 |                                                                                                                                                                                                                                                                                                                                                           |
| `enableCollapse`        | `boolean`                                         | Whether the collapse/expand control is available. When `false` the panel is always expanded, no collapse control is rendered, and `collapsed` / `defaultCollapsed` are ignored. Defaults to `true`.                                                                                                                                                       |
| `collapsed`             | `boolean`                                         | Controlled mode. When supplied, this prop is the source of truth for whether the panel is collapsed and the component keeps no internal state; re-render with a new value in response to `onCollapsedChange`.<br /><br />If both `collapsed` and `defaultCollapsed` are provided, `collapsed` takes precedence. Ignored when `enableCollapse` is `false`. |
| `defaultCollapsed`      | `boolean`                                         | Uncontrolled mode. Seeds the panel's internal collapsed state; the component continues to own the state after mount, so later changes to this prop are ignored.<br /><br />If both `collapsed` and `defaultCollapsed` are provided, `collapsed` takes precedence. Ignored when `enableCollapse` is `false`. Defaults to `false`.                          |
| `onCollapsedChange`     | `(collapsed: boolean) => void`                    | Called whenever the collapsed state changes.                                                                                                                                                                                                                                                                                                              |
| `title`                 | `React.ReactNode`                                 |                                                                                                                                                                                                                                                                                                                                                           |
| `titleIcon`             | `React.ReactNode`                                 |                                                                                                                                                                                                                                                                                                                                                           |
| `showResetButton`       | `boolean`                                         |                                                                                                                                                                                                                                                                                                                                                           |
| `showActiveFilterCount` | `boolean`                                         |                                                                                                                                                                                                                                                                                                                                                           |
| `canReset`              | `boolean`                                         | Whether the reset button is enabled. Hosts compute this from whether the filter and/or visibility state has diverged from its initial snapshot.                                                                                                                                                                                                           |
| `enableSorting`         | `boolean`                                         |                                                                                                                                                                                                                                                                                                                                                           |
| `className`             | `string`                                          |                                                                                                                                                                                                                                                                                                                                                           |
| `renderAddFilterButton` | `() => React.ReactNode`                           |                                                                                                                                                                                                                                                                                                                                                           |

<!-- AUTOGEN:props END -->

### Building blocks

Standalone sub-components used internally by `FilterList`, exported so you can compose them into a custom filter UI.

| Export          | Description                                                                                                                                                    |
| --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `FilterPopover` | Labeled, popover-backed filter trigger that shows a short summary of the current value and mounts its popup contents only while open. Pair with `FilterInput`. |
| `FilterInput`   | OSDK-aware input body for a single filter definition. Renders the right control for the definition's `filterComponent` and drives its filter state.            |

The package also exports a set of utilities for building your own filter UI: `serializeFilterStates` / `deserializeFilterStates` (persist a filter-states map to and from a string, preserving `Date` values), `filterHasActiveState` (whether a state holds an active value), `NO_VALUE` (the sentinel for the null/undefined "No value" bucket), `getFilterKey` / `getFilterLabel` (derive a stable key or display label from a definition), `summarizeFilterValue` (a short summary of a filter's current value for a `FilterPopover` trigger), and `narrowObjectSet` (apply linked-property filters to an `ObjectSet<Q>`).

### Hooks

Headless hook holding the panel's state and where-clause logic. Reach for it when you're building your own filter UI from the building blocks above rather than composing `BaseFilterList`, and want to reuse FilterList's state management and OSDK query composition.

| Export               | Description                                                                                                                                                                                                                                                     |
| -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `useFilterListState` | Owns the filter-states map — seeded from the definitions and `defaultFilterStates` — and composes it into a direct `WhereClause<Q>`, the active linked-property records to apply via `narrowObjectSet`, and per-filter excluding-self clauses for facet counts. |

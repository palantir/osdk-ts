# ontology-explorer-server

This package contains the server for the Ontology Explorer application.
It provides an API to serve the local `ontology.json` file to the app, as well as serving the frontend application.
It can be run as a `npx` command.

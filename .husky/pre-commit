# Check if the git user email is checkpoint@cline.bot
if [ "$(git config user.email)" = "checkpoint@cline.bot" ]; then
  exit 0
fi

node scripts/check-staged-secrets.mjs || exit 1

TERM=dumb npx lint-staged

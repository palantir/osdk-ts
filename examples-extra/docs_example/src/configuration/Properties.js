import React from 'react';

const Properties = () => {
  return (
    <div>
      <h1 className="text-4xl font-bold">Properties</h1>
      <p className="mt-2 text-lg text-gray-600">Typed attributes on object types (string, number, date, etc.).</p>
    </div>
  );
};

export default Properties;

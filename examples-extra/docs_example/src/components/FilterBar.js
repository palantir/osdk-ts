import React from 'react';

const FilterBar = () => {
  return (
    <div>
      <h1 className="text-4xl font-bold">Filter Bar</h1>
      <p className="mt-2 text-lg text-gray-600">Configurable filter interface for narrowing object sets.</p>
    </div>
  );
};

export default FilterBar;

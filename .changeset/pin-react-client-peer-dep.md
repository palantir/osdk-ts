---
"@osdk/react": minor
---

Pin `@osdk/client` peer dep to exact version `2.13.0`. `@osdk/react` is experimental and may ship breaking changes; the prior `^2.8.0` range overstated forward-compat guarantees.

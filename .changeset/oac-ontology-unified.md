---
"@osdk/maker-experimental": minor
"@osdk/maker": minor
"@osdk/generator-converters.ontologyir": minor
"@osdk/generator-converters.preview": patch
"@osdk/vite-plugin-oac": patch
---

add `defineOacOntology` so a single declaration produces OSDK-typed values usable directly with `client(...)` plus the IR consumed by `vite-plugin-oac`. closes the gaps from `DX-CRITIQUE.md`: modify action round-trips both PK-string and Osdk-instance forms, many-to-many links validated end-to-end with a converter integration test, pagination walks via `$nextPageToken`, array/struct/marking properties exercised end-to-end, TS2589 multi-link depth wart removed by stripping link-target metadata, and `defineInterfaceV2` plus `ObjectV2Config.implements` plus `OacOntologyBundle.interfaces` ship together. broad v2/OaC coverage closure: every scalar primitive probed e2e (boolean/byte/date/decimal/double/float/integer/long/short/timestamp), flat-form `PropertyV2Config` variants for string-with-analyzer/decimal-with-precision/marking-with-MANDATORY-subtype, intermediary (3-way junction) links via `defineLinkV2`, optional `status` on every link variant, `defineCreate/Modify/Delete InterfaceObjectActionV2` helpers, missing `defineCreateOrModifyObjectActionV2` e2e, per-parameter and action-level validation surface (allowedValues / required / defaultVisibility / conditionalOverrides), action sections + explicit `parameterOrdering`, `defineInterfaceLinkConstraintV2`, interface `extends` chain exercised e2e (auto-mapping walks the chain so inherited properties get mapped), explicit `{ interface, propertyMapping }` form on `ObjectV2Config.implements`, and `ObjectV2Config.editsHistoryConfig` + basic `datasource: { type: "dataset" }`. downstream `OntologyIrToFullMetadataConverter` now handles intermediary links (MANY/MANY linkSides), `interfaceReference` action parameters (mapped to `interfaceObject`), and `deleteObjectRule` with an interface-typed parameter (emits `deleteInterfaceObject` LogicRule). also fix maker cli to load .mts/.ts ontology files via jiti and read the full ontology block from the ir json, plus fix the plugin's auto-generated modifyObject action impl to dispatch by parameter type when the action uses a semantically-named object parameter (e.g. `passenger`) instead of the canonical `objectToModifyParameter` — previously custom actions declaring `modifiedEntities` would 404 with `primaryKey: "undefined"`. unblocks vite-plugin-oac in a vanilla node 20+ environment without an esm loader flag.

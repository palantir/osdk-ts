---
"@osdk/react-components": patch
---

Restructure the ObjectTable docs into a one-pager — usage, the generated props reference, and a consolidated styling table (per-element `data-*` attributes and `--osdk-table-*` CSS variables) — rendered below the live demo. The extended examples and guidance (data sources, custom columns, sorting, selection, editing, exporting, headless hooks, TypeScript tips, troubleshooting, and theming) move into a linked `ObjectTableExamples.md`.

---
"@osdk/react-components": patch
"@osdk/react-components-styles": patch
---

Fix FilterList styling to match Figma design system: restructure layout with fixed header/footer and scrollable content, add box-shadow, update reset button with outlined border and History icon, update filter item label color to muted, add border-top separator to add-filter footer, style add-filter button with border, style exclude dropdown trigger with blue text, default listogram maxVisibleItems to 5 with styled View all link, scope filter list checkbox to match Figma excluding mode, fix filter state clearing on removal, remove blue outline on selected filters, add rounded border-radius to listogram bar fill, fix date range histogram padding, fix ColumnConfigDialog checkbox padding regression, reorganize BaseTable storybook under ObjectTable/Building Blocks and move WithColumnConfigDialog to Recipes

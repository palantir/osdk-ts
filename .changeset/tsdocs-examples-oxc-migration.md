---
---

Migrate the private `@osdk/typescript-sdk-docs-examples` package to oxlint/oxfmt and stabilize the transpile task hash. Repo tooling only; no published package changes, so this changeset is intentionally empty.

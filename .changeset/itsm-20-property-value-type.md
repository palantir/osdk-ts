---
"@osdk/maker": patch
---

Record the value type an interface property references in the ontology schema lockfile, rejecting a change to which value type it is and warning when the reference is dropped. The version is not recorded, so bumping it is not reported.

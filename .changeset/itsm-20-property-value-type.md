---
"@osdk/maker": patch
---

Record the value type an interface property references in the ontology schema lockfile, rejecting a change to it (including a version bump) and warning when the reference is dropped.

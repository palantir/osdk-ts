---
"@osdk/react-components": patch
---

fix filter-list date histogram x-axis to keep default short-month tick labels when `formatDate` is provided; `formatDate` now only drives the subtitle, tooltip, and From/To picker text

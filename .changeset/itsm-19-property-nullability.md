---
"@osdk/maker": patch
---

Record an interface property's nullability in the ontology schema lockfile, rejecting a change that newly forbids nulls or empty collections, and warning when one is loosened.

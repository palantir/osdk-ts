---
---

Export component documentation in published package for AI agent discoverability. This change adds the `docs/` folder and a `documentation` field to package.json to make component usage guides accessible when the package is installed.

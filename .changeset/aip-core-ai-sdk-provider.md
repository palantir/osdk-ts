---
"@osdk/aip-core": patch
---

Add AI SDK custom provider under `@osdk/aip-core/ai-sdk` implementing LanguageModelV3 with `doGenerate` and `doStream`, enabling usage with the `ai` package's `generateText` and `streamText` functions.

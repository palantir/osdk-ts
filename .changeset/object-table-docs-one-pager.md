---
"@osdk/react-components": patch
---

Restructure the ObjectTable docs into a base-ui-style one-pager: usage, a props reference, and a consolidated styling table (per-element data attributes and `--osdk-table-*` CSS variables), with the styling tokens re-grounded against `table.css`. Add an `ObjectTableExamples.md` page composed from per-feature example chunks in `docs/object-table/examples/` (kept in sync by `scripts/gen-object-table-examples.mjs`).

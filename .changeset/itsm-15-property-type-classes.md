---
"@osdk/maker": patch
---

Record a property's type classes in the ontology schema lockfile, so that adding, dropping or swapping a render hint on an interface property is reported as the breaking change it is.

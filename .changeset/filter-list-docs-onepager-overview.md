---
"@osdk/react-components": patch
---

Add a `FilterListOverview.md` one-pager (usage, a direct `FilterListProps` props reference, a `Style API` table listing every `--osdk-filter-*` CSS variable, and an Advanced section covering `BaseFilterList`, the `FilterPopover`/`FilterInput` building blocks, and the `useFilterListState` hook) rendered by the Storybook FilterList Docs page. Also reword FilterList's docs and JSDoc to say "filter" rather than "narrow" when describing the emitted `ObjectSet`.

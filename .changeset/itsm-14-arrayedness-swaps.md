---
"@osdk/maker": patch
---

Record a property's arrayedness in the ontology schema lockfile, so that adding or dropping `array` on an interface property is reported as the breaking type change it is.

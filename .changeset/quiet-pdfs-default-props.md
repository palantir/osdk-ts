---
"@osdk/react-components": minor
---

Add `defaultPage`, `defaultScale`, `defaultAutoSize`, and `defaultSidebarOpen` to the PDF viewer APIs. The corresponding `initial*` props remain supported as deprecated aliases.

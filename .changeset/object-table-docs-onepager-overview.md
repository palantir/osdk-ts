---
"@osdk/react-components": patch
---

Add an `ObjectTableOverview.md` one-pager (usage, a direct `ObjectTableProps` props reference, and the styling/`Style API` token table) rendered by the Storybook ObjectTable Docs page. The full `ObjectTable.md` reference is unchanged. Marked `draft` so it stays out of the published docs site while iterating.

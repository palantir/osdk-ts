---
"@osdk/maker": patch
---

Record an interface property's primary key constraint in the ontology schema lockfile, rejecting a change that implementing object types would have to be remapped to satisfy, and warning when one is dropped.

---
"@osdk/react-components": minor
---

add `toDerivedNarrowing` to custom FilterList filter definitions so a filter can narrow the object set by runtime derived properties (e.g. "the latest linked row of a given kind has value X"), composed with the other filters' narrowing

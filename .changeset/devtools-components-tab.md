---
"@osdk/react-devtools": patch
---

rebuild the Components tab as a collapsible ontology tree: each mounted component shows the related object types, actions, and properties inferred from its hook usage (object type and action names, not hook names), the instances it read, and its React props, with a health badge, an ontology search box, and a summary of component, object type, and action type counts

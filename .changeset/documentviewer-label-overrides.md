---
"@osdk/react-components": minor
---

Add a `labels` prop to `DocumentViewer` so every user-facing string it owns can be overridden for localization or wording. Any subset can be supplied; unset keys fall back to the built-in English defaults.

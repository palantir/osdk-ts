---
---

Add explicit `docs.source.code` snippets to every Storybook story under `Components/DocumentViewer`. Storybook-only change — no published package is affected, so this changeset is intentionally empty.

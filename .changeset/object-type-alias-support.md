---
"@osdk/functions": minor
"@osdk/client": patch
"@osdk/api": patch
---

Add `Aliases.objectType` and `Aliases.query` for remapping object type, property and query api names via resources.json, so the same built code can run against a stack where they are named differently
